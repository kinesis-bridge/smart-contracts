# Kinesis Bridge Smart contracts

*To be completed*

## Shell scripts for pact side

``./shell/gen_tokens.sh``: Gen the kb-tokens from template
``./shell/gen_target``: Build the final pact contracts to be deployed to the correct namespace
``./shell/test.sh``: Launch the pact tests
